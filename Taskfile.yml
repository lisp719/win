version: "3"

tasks:
  default:
    desc: "Default task from justfile"
    cmds:
      - echo 'Hello, world!'

  check-archlinux:
    desc: "ansible-playbook --check --diff in archlinux"
    dir: archlinux
    cmds:
      - ansible-playbook wsl.yaml --check --diff

  run-archlinux:
    desc: "ansible-playbook -v in archlinux"
    dir: archlinux
    cmds:
      - ansible-playbook wsl.yaml -v

  check-fedora:
    desc: "ansible-playbook --check --diff in fedora"
    dir: fedora
    cmds:
      - ansible-playbook wsl.yaml --check --diff

  run-fedora:
    desc: "ansible-playbook -v in fedora"
    dir: fedora
    cmds:
      - ansible-playbook wsl.yaml -v

  check-ubuntu:
    desc: "ansible-playbook --check --diff in ubuntu"
    dir: ubuntu
    cmds:
      - ansible-playbook wsl.yaml --check --diff

  run-ubuntu:
    desc: "ansible-playbook -v in ubuntu"
    dir: ubuntu
    cmds:
      - ansible-playbook wsl.yaml -v
